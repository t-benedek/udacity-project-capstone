apiVersion: apps/v1
kind: Deployment
metadata:
  name: quarkus-hello
  labels:
    app: quarkus-hello
spec:
  replicas: 5
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: quarkus-hello
        image: tbenedek/quarkus-hello
        ports:
        - containerPort: 8080